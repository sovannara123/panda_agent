global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'panda-agent'
    metrics_path: '/metrics'
    # If API key is enabled in .env, pass the Authorization header here
    # bearer_token: 'your_api_key_here'
    static_configs:
      - targets: ['panda-api:8000']
